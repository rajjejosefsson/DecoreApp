@model Decore.ClientApp.ViewModels.EventViewModel

@{
    ViewBag.Title = "Event Page";
}


<style>


  
    .event {
        margin-bottom: 30px;
    }

    .thumbnail {
        background-color: #FFF;
        -webkit-box-shadow: 0px 0px 19px 0px rgba(0,0,0,0.16);
        -moz-box-shadow: 0px 0px 19px 0px rgba(0,0,0,0.16);
        box-shadow: 0px 0px 19px 0px rgba(0,0,0,0.16);
        border-radius: 4px;
        color: #444444;
    }
</style>


<div class="content" style="display: block">
    @foreach (var item in Model.Events.OrderByDescending(Model => Model.CreatedAt))
    {
        <div class="row event" id="myTable">
            <div class="col-sm-8 col-sm-offset-2 col-md-8 col-md-offset-2">
                <div class="thumbnail">
                    <img src="@item.ImageURL" alt="event image">
                    <div class="caption">
                        <h3>@item.Title</h3>
                        <p>Beskrivning: @item.Description</p>
                        <p>Event typ:
                        @foreach (var eventType in @Model.EventTypes)
                        {
                            if (eventType.Id == @item.EventTypeId)
                            {
                                 @eventType.Title
                            }
                        }
                        </p>
                       
                        <p>Adress: @item.Adress</p>
                        <p>Pris på biljett: @item.BasePrice kr</p>
                        <p>Startdatum för event: @item.StartDate</p>
                        <p>Slutdatum för event: @item.EndDate</p>
                        <p>Slutdatum för försäljning: @item.SaleStop</p>
                        <p>Skapad: @item.CreatedAt</p>
                        <p><a href="#" class="btn btn-primary" role="button">Köp Biljett</a> <a href="#" class="btn btn-default" role="button">Personal</a> <a href="" class="btn btn-danger deleteButton" role="button" data-id="@item.Id">Ta Bort</a></p>                     
                    </div>
                </div>
            </div>
        </div>
    }
</div>
@section scripts {
    
    <script>

        
        $("#myTable")
            .on("click",
                ".deleteButton",
                function() {
                  
                    var collapsedDiv = $(this).closest('div');
                    var partedDiv = collapsedDiv.prev('div.parent');

                    // Remove collapsed div
                    collapsedDiv.remove();
                    partedDiv.remove();

                    var eventId = $(this).data('id');

                    $.ajax({
                        type: "post",
                        url: "@Url.Action("DeleteEventById", "EventList")",
                        ajaxasync: true,
                    data: { id: eventId },

                    success: function() {
                        alert("success");
                    },
                    error: function(data) {
                        alert(data.x);
                    }
                });
        });

        








    </script>

















    }
