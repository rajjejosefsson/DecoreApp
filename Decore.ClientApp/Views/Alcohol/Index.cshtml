
@using System.Web.Mvc
@using System.Web.Mvc.Html
@model Decore.ClientApp.ViewModels.BeverageViewModel

@{
    ViewBag.Title = "Alcohol Page";
}

<style>


</style>



<div class="row content">
    <div class="col-sm-12 col-md-8 col-md-offset-2">
        <div class="form-box">
            <h2 class="text-center">Uppvägning</h2>
            <div class="inner-form">


                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-group">
                        @Html.Label("Beverage", "Dryck")
                        @Html.DropDownListFor(model => model.Beverages, new SelectList(Model.Beverages, "BeverageId", "Title"), new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.Label("Measure", "Enhet för uppvägning")
                        @Html.DropDownListFor(model => model.Measures, new SelectList(Model.Measures, "MeasureId", "MeasureTYPE"), new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.Beverage.Volume, "Volym")
                        @Html.TextBoxFor(model => model.Beverage.Volume, new { @class = "form-control", id = "volume" })
                    </div>


                    <!-- TODO: INTE FIXAD ÄNNU -->

                    <div class="form-group">
                        <label for="usr">Pris:</label>
                        <input type="text" class="form-control" id="usr">
                    </div>

                    <button type="button" class="btn btn-primary">Lägg till</button>


                    <hr />


                    <!-- Exempel men mycket mer kod

                    <div class="form-group">
                        @Html.Label("dryck", "Dryck:")
                        <select name="myFormInputName" id="dryck" class="form-control">
                            @foreach (var beverage in Model.Beverages)
                            {
                                <option value="@(beverage.BeverageId)">@(beverage.Title)</option>
                            }
                        </select>
                    </div>

                    -->




                    <div>
                        <p>Carlsberg, 33cl, 5%, 19,90:- (30 st)</p>
                        <p>Tequila, 40%, 199:- (1500g) </p>
                        <p>Viko cider, 33cl, 4.5%, 11.50:- (20 st)</p>
                    </div>

                    <button type="submit" class="btn btn-success">Klar</button>





                }


            </div>
        </div>
    </div>

</div>

<!-- EN KOMMENTAR -->
<!--test-->

